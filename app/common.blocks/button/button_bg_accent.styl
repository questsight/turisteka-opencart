.button_bg_accent
  background accentColor
  color lightColor
  transition-duration 0.35s
  &:active
    color lightColor
  &:focus
    color lightColor
  &:hover
    background darken(accentColor, changeColor)
    color lightColor
